services:
  generic-study-app:
    build: .
    container_name: generic-study-app
    ports:
      - "4173:4173"
    env_file:
      - .env
    environment:
      - DEBUG_LOG=false
    restart: unless-stopped